// Inicializar la librería de animaciones AOS
AOS.init({
    duration: 1000, /* La animación dura 1 segundo, haciéndola suave */
    once: true,     /* La animación solo ocurre la primera vez que el usuario baja */
    offset: 100     /* Las tarjetas aparecen un poco antes de estar completamente en pantalla */
});